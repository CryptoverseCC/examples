{
  "name": "boost",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://userfeeds.github.io/examples/examples/boost/",
  "description":
    "Boosting - one of the monetization methods that can be incorporated into almost any interface. Pay for putting elements of your interface to the most visible spot",
  "dependencies": {
    "@userfeeds/core": "^0.1.2",
    "@userfeeds/button": "^0.0.17",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4",
    "web3": "1.0.0-beta.29"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
